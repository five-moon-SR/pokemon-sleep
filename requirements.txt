# 依存の内容が変わると Streamlit Cloud は環境を作り直す＝プロセスが本当に再起動する。
# 2026-08-24: selectbox互換パッチ反映のためCloud環境の再構築を促す。
# 「ページは新しいのに import 済みモジュールが古い」状態から抜けられない時は、
# ここを触るのが確実な手段（Reboot ボタンではプロセスが残ることがある）。
streamlit>=1.58,<2
pandas>=2.0
psycopg2-binary>=2.9
